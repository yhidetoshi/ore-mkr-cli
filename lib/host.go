package oremkrcli

import "fmt"

//func FetchHostInfo(mkr *client) {


func FetchHostInfo() {
/*
	hostId, _ := client.FindHosts(
		&mackerel.FindHostsParam{Statuses: []string{"working", "standby", "maintenance", "poweroff"}})
	for _, v := range hostId {
		fmt.Println(v.Name)
	}
*/
	fmt.Println("aaa")
}

